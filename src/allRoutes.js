//Navbar Routes
const home = "/fraudnet-ui/home";

const models = "/fraudnet-ui/model";

const riskMgmt = "/fraudnet-ui/modelManagement";

const lists = "/fraudnet-ui/lists";

const org = "/fraudnet-ui/organization";

const Users = "/fraudnet-ui/user";

const guis = "/fraudnet-ui/dashboard/list";

const Reports = "/fraudnet-ui/reporting";

const documentation = "/fraudnet-ui/doc";

const support = "/fraudnet-ui/support";

//lists routes

const posNegList = "/fraudnet-ui/list/posneg";

const exception = "/fraudnet-ui/list/exception";

const riskyAirports = "/fraudnet-ui/list/risky-airport";

const riskyProducts = "/fraudnet-ui/list/risky-products";

const ofac = "/fraudnet-ui/list/ofac";

const countries = "/fraudnet-ui/list/domestic-countries";

const domains = "/fraudnet-ui/list/free-domains";

export {
  home,
  models,
  documentation,
  support,
  riskMgmt,
  guis,
  Users,
  Reports,
  lists,
  org,
  posNegList,
  exception,
  riskyAirports,
  riskyProducts,
  ofac,
  countries,
  domains,
};
